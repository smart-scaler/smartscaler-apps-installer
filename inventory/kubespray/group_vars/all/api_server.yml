---
# API Server Configuration - fallback values if not set by deploy_kubernetes.yml
# The actual values will be set dynamically by our deployment script

# Fallback values only - these will be overridden by deploy_kubernetes.yml
kube_apiserver_ip: "127.0.0.1"
kube_apiserver_port: 6443
supplementary_addresses_in_ssl_keys: ["127.0.0.1"]

# Ensure API server binds to the correct IP
bind_address: "127.0.0.1"
advertise_address: "127.0.0.1" 